---

interface Props {
    count: number,
}

const {count} = Astro.props

---

<p style={`translate: -${(3) * 10}px`} class="w-12 font-bold font-mono text-xl  justify-center text-center flex items-center bg-slate-200 h-12 aspect-square rounded-full">
+{count}
</optgroup>